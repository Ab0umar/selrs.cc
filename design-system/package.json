{
  "name": "selrs-ui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist --external react --external react-dom"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "tsup": "^8.5.1"
  }
}
